{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "A sample json server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "Arunsridher",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
